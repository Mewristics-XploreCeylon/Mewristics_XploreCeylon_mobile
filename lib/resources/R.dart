import '../constants/colors.dart';
import '../constants/sizes.dart';

class R {
  static AppColors colors = AppColors();
  static AppSize appSize = AppSize();
  static AppPadding appPadding = AppPadding();
}